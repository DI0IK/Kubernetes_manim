apiVersion: apps/v1
kind: Deployment
metadata:
  name: cluster-autoscaler
  namespace: kube-system
spec:
  template:
    spec:
      containers:
      - image: cluster-autoscaler:v1.30
        command:
        - ./cluster-autoscaler
        - --nodes=2:10
        - --scale-down-enabled=true